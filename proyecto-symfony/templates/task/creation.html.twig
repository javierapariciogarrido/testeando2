{% extends 'base.html.twig' %}

{% block title %}
    {% if edit is defined %}
        Editar tarea 
    {% else %}
        Crear Tarea
    {% endif%}    
{% endblock %}

{% block body %}
    <div class="example-wrapper">
        {% if edit is defined %}
            <h2>Editar tarea</h2>
        {%else%}
            <h2>Crear Tarea</h2>
        {% endif %}    
        {{form_start(form)}}
        {{form_widget(form)}}
        {{form_end(form)}}
        
        {#Boton volver atras#}
        
        <a class="tareas-acabadas" href="{{path('tasks',{'tareasacabadas':'NO','ente':'particular'})}}">Volver a mis tareas</a>
           
    </div>
{% endblock %}
